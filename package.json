{
  "name": "paint-ithaca",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A camera, a hold, a watercolor moment.",
  "scripts": {
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
